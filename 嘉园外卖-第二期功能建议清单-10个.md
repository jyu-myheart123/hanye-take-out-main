# 嘉园外卖商家管理平台 — 第二期可更新功能建议清单（10 个）

> 整理人：231110130 - 陈越涛
> 日期：2026-09-12
> 说明：以下 10 个功能均基于第一期已完成的技术底座设计（Spring Boot 3 + MySQL 8 + Redis + WebSocket + Vue 3 + Element Plus + 阿里云 OSS + AI 助手），**不更换技术栈、不推翻现有代码**，按"痛点 → 功能 → 实现方案 → 价值"描述，可直接排期开发。

---

## 功能一：智能沽清（一键售罄 + 全端实时同步 + 替代菜推荐）

**解决的商家痛点**
店里某道菜卖完了，但外卖小程序还能下单，接了单才发现做不了，只能打电话给顾客道歉退单，非常伤口碑。

**具体功能**
- 菜品管理页每个菜加一个「沽清」开关，一键标记今日售罄；也支持"今日限量 5 份"，卖完自动沽清
- 沽清后：外卖端（后期小程序）该菜立即变灰不可点；**后台堂食开单页和 AI 助手里也自动隐藏/提示**
- 顾客点了沽清菜时，自动推荐 2~3 道同分类、同价位的替代菜
- 每天凌晨自动重置沽清状态（第二天备货后恢复售卖）

**实现方案（可落地）**
- Redis 存当日沽清集合：`dish:soldout:{日期}`（Set 结构存菜品 id），限量份数用 `dish:stock:{id}` 计数，下单时用 Redis 原子扣减
- 复用现有 WebSocket：沽清瞬间推送给所有已打开的开单页面，无需刷新
- 后端加 2 个接口：`PUT /admin/dish/soldout/{id}`、`PUT /admin/dish/stock`；Spring Task 定时任务每天 00:05 清 key
- 替代菜：SQL 按同 `category_id`、价格 ±10 元区间、启售状态取销量最高的 3 道

**难度**：⭐⭐（约 2 天）
**商家价值**：杜绝"接单后做不了"的客诉，高峰期操作只需点一下。

---

## 功能二：漏单预警与超时订单自动处理

**解决的商家痛点**
饭点最忙的时候，新订单弹窗一闪而过、语音播报被嘈杂声盖住，漏看一单就是差评 + 平台罚款。

**具体功能**
- 新单进来后若 **3 分钟无人接单**，后台全屏红色闪烁 + 持续急促语音"您有新订单未处理，请及时接单"
- 可配置"超时 N 分钟自动拒单"（开关 + 时长商家自定），避免长时间挂单
- 订单卡片上显示倒计时进度条，剩余时间一目了然
- 已接单订单超过预估出餐时间未完成，再次预警催厨

**实现方案（可落地）**
- 后端用 **Redis 过期监听（Keyspace Notifications）** 或定时任务扫描 `status=待接单 且 下单时间超阈值` 的订单，超时后通过现有 WebSocket 通道推强提醒消息
- 新增配置表 `shop_config`（键值对：超时秒数、是否自动拒单），店铺设置页维护
- 前端复用现有 WebSocket 连接，新增消息类型 `type=3`（超时预警），触发全屏遮罩闪烁组件
- 自动拒单复用现有拒单 Service 方法，包一层定时任务调用即可

**难度**：⭐⭐（约 2 天）
**商家价值**：把"靠人盯屏幕"变成"系统盯着人"，忙时零漏单。

---

## 功能三：后厨 KDS 分单系统 + 小票/叫号打印

**解决的商家痛点**
前台手写小票送后厨，字迹潦草易漏单；凉菜、热菜、饮品由不同档口制作，一张单子传来传去效率低。

**具体功能**
- 接单后按档口自动分单：热菜→后厨屏、饮品→水吧屏、凉菜→冷菜屏，各档口只看自己的菜
- 厨房大屏（KDS）：大字显示桌号、菜品、份数、备注（"不要辣"飘红），出餐后点"完成"，前台实时看到进度
- 支持云打印机（飞鹅/易联云，HTTP API 调用）自动出小票；堂食单出餐后大屏叫号"请 A12 号取餐"
- 催菜按钮：前台一键催单，后厨对应卡片变红置顶

**实现方案（可落地）**
- 菜品表加 `kitchen_type` 字段（1热菜/2凉菜/3饮品…），分单逻辑在接单 Service 里按字段拆分组
- KDS 大屏就是一个新的 Vue 全屏页面 `/kds`，走现有 WebSocket 实时推送，状态回写 orders_detail 表加 `make_status` 字段
- 云打印机只需后端发一个 HTTP 请求到厂商开放接口（项目已能联网调阿里云 OSS，模式相同），小票内容用字符串模板拼接
- 不买打印机也能用：KDS 大屏 + 浏览器语音叫号（`speechSynthesis`，与 AI 助手语音同属浏览器原生能力，零成本）

**难度**：⭐⭐⭐（约 4 天，接云打印机另需购买设备）
**商家价值**：前后厨信息零延迟，省掉传单人工，小店直接用大屏不买打印机也能跑。

---

## 功能四：交接班与收银对账（班次管理）

**解决的商家痛点**
堂食现金单、线下支付多，员工换班时全靠口算"我这班上收了多少钱"，账对不上只能扯皮。

**具体功能**
- 员工点"开班"开始记录本班次，"交班"时自动生成对账单：
  - 本班订单数、总营业额（堂食/外卖分开）
  - 线下现金收了多少、微信线上收了多少（后期接支付后自动归集）
  - 退款/作废单数及金额
- 交班需下一任员工确认接收，双方留痕，谁上的班、收了多少钱清清楚楚
- 班次历史可查，老板随时翻任意一天的交接班记录

**实现方案（可落地）**
- 新建 `work_shift` 表：id、开班人id、开班时间、交班人id、交班时间、本班订单汇总（冗余金额字段，交班时一次性算好存快照）
- 订单与班次关联：orders 表加 `shift_id`，提交订单时取 Redis 中当前"进行中的班次 id"写入（Redis key `shift:current:{员工id或门店id}`）
- 交班接口里用一条 SQL 聚合：`SELECT count(*), sum(amount) FROM orders WHERE shift_id=?` 按支付方式分组
- 前端新增"交接班"菜单页，Element Plus 弹窗做开班/交班确认，历史记录用现有表格分页组件，开发很快

**难度**：⭐⭐（约 2~3 天）
**商家价值**：堂食店每天必用的刚需，账实清晰，员工之间不扯皮。

---

## 功能五：堂食会员体系（手机号会员 + 储值 + 积分）

**解决的商家痛点**
需求里提到很多"堂食老客户"，但系统现在对老客户没有任何识别和留存手段，老客和新客一个价，没有复购抓手。

**具体功能**
- 堂食开单时输入手机号自动识别会员：显示姓名、等级、余额、积分
- 会员注册：消费即自动建档（手机号 + 首次消费时间）
- 储值：后台给会员充值（如充 200 送 20），结账可扣余额
- 积分：按消费额自动累计（如 1 元 = 1 分），积分可抵扣（100 分抵 5 元，规则商家自配）
- 会员等级（银卡/金卡/钻石）按累计消费自动升级，不同等级不同折扣
- 会员消费记录可查

**实现方案（可落地）**
- 新建 3 张表：`member`（id、姓名、手机号、余额、积分、等级、累计消费、创建时间）、`member_flow`（余额/积分变动流水：充值/消费/退款）、`member_level_rule`（等级门槛与折扣）
- 堂食提交接口（现有 `dineInSubmit`）扩展：可选传 phone，后端校验会员→按等级算折扣→扣余额/加积分，全部在同一个数据库事务里完成
- 余额扣减必须用乐观锁：`UPDATE member SET balance=balance-? WHERE id=? AND balance>=?`，防止并发超扣
- AI 助手顺带升级：语音说"138 开头那位老顾客的会员价"就能查（复用现有规则引擎）

**难度**：⭐⭐⭐（约 4 天）
**商家价值**：把一次性顾客沉淀成可反复触达的会员，储值还能提前回笼资金。

---

## 功能六：营销活动引擎（满减 / 折扣 / 优惠券 / 第二份半价）

**解决的商家痛点**
闲时（如下午 2~5 点）没客人，想做活动拉销量，但活动规则写在纸上、靠收银员手动算，经常算错。

**具体功能**
- 商家在后台可视化配置活动，无需改代码：
  - **满减**：满 30 减 5、满 60 减 12，可叠加多档
  - **折扣**：全场 8.8 折 / 指定分类 9 折 / 指定会员价
  - **第二份半价**：买一送一、第二份立减
  - **优惠券**：生成券码，会员领取，下单核销
- 可限制活动时间段（只在闲时生效）、适用菜品范围
- 下单时系统自动计算最优优惠，员工/顾客零操作；订单详情展示优惠明细
- 活动效果分析：发了多少券、用了多少、带动多少营业额

**实现方案（可落地）**
- 新建 `promotion` 表（类型、规则 JSON、起止时间、适用范围、状态）+ `coupon`（券码、会员id、状态、有效期）
- 后端写独立的 `DiscountCalculator` 策略类：每种优惠一个 Calculator 实现，下单 Service 统一调用，与现有"后端核算金额"的安全设计一脉相承——优惠也绝不信前端传值
- 同一订单多活动的取舍：按"商家让利最少"或"顾客最优惠"由配置决定，计算过程可加日志便于核对
- 前端活动配置页用 Element Plus 表单 + 时间选择器；效果分析复用 statistics 模块的图表

**难度**：⭐⭐⭐（约 4~5 天，建议先做满减一种跑通再扩展）
**商家价值**：闲时引流、提升客单价的核心武器，且全自动算账不出错。

---

## 功能七：经营日报定时自动推送（老板手机上看店）

**解决的商家痛点**
老板不可能 24 小时守在后台，每天打烊后想知道"今天卖了多少、什么菜最好卖、有没有差评"，得自己打开电脑翻。

**具体功能**
- 每天固定时间（如 22:00，可配置）自动生成当日经营日报并推送到老板手机：
  - 今日营业额、订单数、客单价、环比昨日涨跌
  - 销量 TOP5 / 滞销 TOP5
  - 退款漏单数、会员新增数
- 推送渠道（任选，全部免费或低成本）：
  - **企业微信群机器人 Webhook**（最简单，一个 HTTP POST 搞定，带 Markdown 卡片）
  - 钉钉群机器人 / 飞书机器人（同理）
  - 邮件（Spring Boot 自带 `spring-boot-starter-mail`）
- 周报：每周一早上推送上周汇总
- 异常告警：当日营业额同比跌超 30% 时主动推送预警

**实现方案（可落地）**
- 纯后端功能：Spring Task 定时任务（项目已引入 Spring Boot，加 `@EnableScheduling` 即可，无需新依赖）
- 数据全部来自现有 statistics 模块的统计 SQL，抽成 Service 复用
- Webhook 推送就是后端发一个 POST JSON 请求，和现在调云 OSS 的 HTTP 调用模式一样
- 后台加一个"通知设置"页配置 Webhook 地址和推送时间，存 shop_config 表

**难度**：⭐⭐（约 2 天）
**商家价值**：老板离店也能心中有数，是小店老板感知最强的"高级感"功能。

---

## 功能八：销量分析与智能备货建议

**解决的商家痛点**
备多了食材浪费赔钱，备少了早早沽清丢生意。每天备多少菜全凭老师傅感觉，人一请假就抓瞎。

**具体功能**
- 菜品销量分析：近 7/30 天每道菜的销量曲线、星期几卖得多（很多店周五和周一销量差 40%）
- **明日备货建议**：取过去 4 个同星期几的销量加权平均（越近权重越高），给出建议备货份数区间（保守值/推荐值/激进值）
- 天气联动（加分项）：接免费天气 API，下雨/降温自动在建议值上浮动（外卖雨天通常单多）
- 滞销预警：连续 7 天销量低于阈值的菜品标红，建议下架或做活动
- 备货单一键导出 Excel / 打印，早上发给采购

**实现方案（可落地）**
- 数据源就是现有 orders_detail + orders 表，一条聚合 SQL 按 `dish_id + 日期 + 星期` 分组统计
- 预测算法先不用机器学习，**加权移动平均**足够准：`(近4周同星期 × [0.4,0.3,0.2,0.1])`，在 Service 里几十行代码实现；后期想升级再换模型
- 前端复用 statistics 页的 ECharts 画趋势线，备货建议用表格 + 置信度标签
- 导出复用现有 `exportInforAPI` 同款 POI/EasyExcel 方案（项目报表导出已有依赖）

**难度**：⭐⭐⭐（约 3 天）
**商家价值**：直接帮老板省食材成本、少丢单，省钱就是赚钱。

---

## 功能九：实时经营数据大屏（电视挂墙模式）

**解决的商家痛点**
店里想挂个电视展示经营情况给员工看气势、给顾客看热度（"这家店销量这么高一定好吃"），现在没有一个能全屏展示的页面。

**具体功能**
- 一键进入全屏大屏（也可在平板/电视浏览器常开）：
  - 今日实时营业额大数字跳动、订单数、客单价
  - 实时订单滚动流（最新订单从右侧飞入，脱敏展示）
  - 今日销量 TOP10 菜品横向条形图，实时增长动画
  - 堂食/外卖占比环形图、时段营业曲线
  - 打烊后自动切换"今日战报"总结页
- 深色科技风配色，数字滚动动画，适配 1920×1080 电视

**实现方案（可落地）**
- 纯前端新页面 `/screen`（一个独立路由，不套现有侧边栏布局），数据接口复用 statistics 模块现有接口
- 实时性：复用现有 WebSocket 连接，每来一单数字和图表自动更新；再用 `setInterval` 每 60 秒兜底拉一次
- 图表继续用项目已有的 ECharts；数字滚动用 `vue3-count-to` 或自己写 requestAnimationFrame 动画
- 无需新后端接口，成本极低但展示效果非常震撼，适合答辩演示

**难度**：⭐⭐（约 2~3 天）
**商家价值**：低成本高显示度，既给团队打气也是答辩/展示的亮点功能。

---

## 功能十：AI 经营顾问升级（自然语言查数据 + 经营诊断）

**解决的商家痛点**
现在的 AI 助手能开单、导报表、切状态，但老板想看"上周几道菜卖得最差""这个月比上个月赚得多吗"还得自己翻图表找。

**具体功能**（在现有 AI 助手上原地升级）
- **自然语言查数**：直接问——
  - "上周卖得最好的 5 道菜"
  - "这个月营业额比上个月多还是少？"
  - "今天有几个退款单？"
  - "会员里谁消费最高？"
  AI 返回结构化答案（数字 + 迷你图表卡片）
- **经营诊断**：每天首次打开主动推送 3 条洞察，如"鱼香肉丝连续 5 天销量下滑 30%，建议关注口味或备货""晚市 6 点出餐时长比平时多 8 分钟，建议增加人手"
- **语音录菜**：对着 AI 助手说"新增一道菜叫酸辣土豆丝，卖 12 块，分类是热菜"，自动填表，员工确认后入库
- 对话中可追问（"那上周呢？""换成只看堂食"），支持上下文

**实现方案（可落地）**
- 现有 AI 助手是"规则引擎"，升级为 **规则引擎 + 参数化查询模板**两层：
  - 第一层：正则/关键词把问题映射到预置的 20~30 个"查询模板"（销量TOP、环比、退款数……），模板里的时间范围、数字从问句中解析——这部分不依赖大模型，零成本可实现 80% 的常见问法
  - 第二层（可选加分）：接 DeepSeek/通义千问等国产大模型 API（几元钱百万 token），用 Function Calling 把"查询模板"注册成工具，让大模型决定调哪个模板、传什么参数，回答自然语言由大模型生成——数据始终来自自己的接口，**不让大模型直接碰数据库**，安全可控
- 统计 SQL 全部复用 statistics 模块；语音用现有 `webkitSpeechRecognition`；新增菜品复用现有菜品新增接口
- 上下文用一个简单的 messages 数组（现有对话已有），追问时把上一轮解析出的条件（时间、范围）带过去

**难度**：⭐⭐⭐（规则版 3 天；加大模型版 +2 天）
**商家价值**：让整个后台从"人找功能"进化为"功能找人"，是全系统最有记忆点的差异化功能。

---

## 优先级建议（先做哪个）

| 优先级 | 功能 | 理由 |
| --- | --- | --- |
| 🔴 第一批（刚需、成本低） | ① 智能沽清、② 漏单预警、④ 交接班对账 | 每天都用，直接减少客诉和错账，各 2~3 天 |
| 🟡 第二批（拉营收） | ⑤ 会员体系、⑥ 营销活动、⑧ 备货建议 | 帮商家多赚钱、少浪费，可配套宣传 |
| 🟢 第三批（提效与亮点） | ③ 后厨 KDS、⑦ 日报推送、⑨ 数据大屏、⑩ AI 顾问升级 | 管理提效 + 演示效果好，其中数据大屏和日报推送成本最低 |

> 小建议：如果用于课程答辩，推荐组合 **①+②+⑨+⑩**——开发量小（约 9 天）、现场演示效果最炸，且全部复用现有 WebSocket、统计接口和 AI 助手底座，代码风险最低。
