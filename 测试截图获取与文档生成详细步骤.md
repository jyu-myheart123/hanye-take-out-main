# 测试截图获取与文档生成详细步骤

> **适用人群**：小白开发者、测试新手
> **目标**：完整获取所有测试环节的运行截图，并正确插入到测试文档中
> **时间预估**：2-3小时

---

## 一、准备工作

### 1.1 确认环境已安装

请确保您的电脑上已安装以下软件：

| 软件 | 版本要求 | 安装路径参考 |
|------|----------|--------------|
| JDK | 17 | `D:\java\jdk17.0.15` |
| Maven | 3.6.1+ | `D:\java\apache-maven-3.6.1` |
| MySQL | 8.0.39 | `C:\Program Files\MySQL\MySQL Server 8.0` |
| Redis | 7.x | `D:\redis\redis-install-path` |
| Node.js | 16+ | 任意路径 |
| Python | 3.10+ | 任意路径 |
| JMeter | 5.6.3 | 任意路径 |

### 1.2 确认环境变量已配置

按 `Win + R`，输入 `cmd`，回车打开命令提示符，依次执行以下命令：

```cmd
java -version
mvn -version
node -v
python -V
```

如果都能正常显示版本号，说明环境配置正确。

### 1.3 创建截图保存目录

打开文件资源管理器，在项目根目录下创建以下目录结构：

```
hanye-take-out-main/
└── screenshots/
    ├── 01_项目启动/
    ├── 02_单元测试/
    ├── 03_功能测试/
    ├── 04_接口测试/
    ├── 05_UI测试/
    ├── 06_性能测试/
    ├── 07_安全测试/
    ├── 08_CI集成/
    └── 09_页面展示/
```

**操作步骤**：
1. 打开项目根目录：`d:\small-third\project1\hanye-take-out-main`
2. 右键空白处 → 新建 → 文件夹，命名为 `screenshots`
3. 进入 `screenshots` 文件夹，创建上述9个子文件夹

### 1.4 截图工具推荐

| 工具 | 推荐度 | 使用方法 |
|------|--------|----------|
| Windows 自带截图 | ⭐⭐⭐⭐⭐ | `Win + Shift + S`，选择区域后自动保存到剪贴板 |
| Snipaste | ⭐⭐⭐⭐⭐ | 截图后可悬浮显示，方便标注 |
| FastStone Capture | ⭐⭐⭐⭐ | 专业截图工具，支持编辑 |

**推荐使用 Windows 自带截图**，简单方便，步骤如下：
1. 按下 `Win + Shift + S`
2. 屏幕会变暗，出现截图工具栏
3. 选择"矩形截图"或"全屏截图"
4. 截图后自动保存到剪贴板
5. 打开"画图"或直接在文档中粘贴

---

## 二、项目启动截图（01_项目启动）

### 2.1 启动 MySQL 数据库

**步骤**：
1. 按 `Win + R`，输入 `services.msc`，回车
2. 在服务列表中找到 `MySQL80`（或类似名称）
3. 右键 → 启动

**截图要点**：
- 截图1：服务管理器中 MySQL 状态为"正在运行"
- 文件名：`01_01_mysql启动.png`

### 2.2 启动 Redis

**步骤**：
1. 打开命令提示符（CMD）
2. 进入 Redis 安装目录：
   ```cmd
   cd D:\redis\redis-install-path
   ```
3. 启动 Redis：
   ```cmd
   redis-server.exe redis.windows.conf
   ```

**截图要点**：
- 截图2：Redis 启动成功界面，显示 "Ready to accept connections"
- 文件名：`01_02_redis启动.png`

### 2.3 启动后端服务

**步骤**：
1. 打开命令提示符（CMD）
2. 进入后端项目目录：
   ```cmd
   cd d:\small-third\project1\hanye-take-out-main\hanye-take-out-springboot3\server
   ```
3. 编译项目：
   ```cmd
   mvn clean package -DskipTests
   ```
4. 启动服务：
   ```cmd
   java -jar target/hanye-take-out-server-1.0-SNAPSHOT.jar
   ```

**截图要点**：
- 截图3：Maven 编译成功界面，显示 "BUILD SUCCESS"
- 截图4：后端服务启动成功界面，显示 "Started TakeOutApplication"
- 文件名：`01_03_maven编译成功.png`、`01_04_后端启动成功.png`

### 2.4 启动前端（商家后台）

**步骤**：
1. 打开新的命令提示符（CMD）
2. 进入前端项目目录：
   ```cmd
   cd d:\small-third\project1\hanye-take-out-main\hanye-take-out-vue3
   ```
3. 安装依赖（首次运行）：
   ```cmd
   npm install --ignore-scripts
   ```
4. 启动前端：
   ```cmd
   npm run dev
   ```

**截图要点**：
- 截图5：前端启动成功界面，显示"VITE"图标和访问地址
- 截图6：浏览器访问 `http://localhost:5173`，显示登录页面
- 文件名：`01_05_前端启动成功.png`、`01_06_登录页面.png`

### 2.5 登录商家后台

**步骤**：
1. 打开浏览器，访问 `http://localhost:5173`
2. 输入账号：`cyh`，密码：`123456`
3. 点击登录

**截图要点**：
- 截图7：登录成功后显示的仪表盘页面
- 文件名：`01_07_仪表盘首页.png`

---

## 三、单元测试截图（02_单元测试）

### 3.1 运行单元测试

**步骤**：
1. 打开命令提示符（CMD）
2. 进入后端项目目录：
   ```cmd
   cd d:\small-third\project1\hanye-take-out-main\hanye-take-out-springboot3\server
   ```
3. 运行所有单元测试：
   ```cmd
   mvn test
   ```

**截图要点**：
- 截图1：单元测试运行中，显示各测试类的执行进度
- 截图2：单元测试全部通过，显示 "Tests run: 84, Failures: 0"
- 文件名：`02_01_单元测试运行中.png`、`02_02_单元测试全部通过.png`

### 3.2 生成代码覆盖率报告

**步骤**：
1. 在后端项目目录执行：
   ```cmd
   mvn jacoco:report
   ```
2. 打开报告文件：
   ```
   server/target/site/jacoco/index.html
   ```

**截图要点**：
- 截图3：Jacoco 覆盖率报告首页，显示整体覆盖率
- 截图4：各包的覆盖率详情
- 文件名：`02_03_jacoco覆盖率报告.png`、`02_04_包覆盖率详情.png`

---

## 四、功能测试截图（03_功能测试）

### 4.1 菜品管理功能测试

**步骤**：
1. 登录商家后台
2. 点击左侧菜单"菜品管理"
3. 查看菜品列表
4. 点击"添加菜品"按钮
5. 填写菜品信息并保存

**截图要点**：
- 截图1：菜品列表页面
- 截图2：添加菜品表单页面
- 截图3：菜品添加成功提示
- 文件名：`03_01_菜品列表.png`、`03_02_添加菜品表单.png`、`03_03_菜品添加成功.png`

### 4.2 订单管理功能测试

**步骤**：
1. 点击左侧菜单"订单管理"
2. 查看不同状态的订单
3. 操作订单（接单、派送、完成）

**截图要点**：
- 截图4：订单列表页面
- 截图5：订单详情弹窗
- 截图6：订单状态变更操作
- 文件名：`03_04_订单列表.png`、`03_05_订单详情.png`、`03_06_订单状态变更.png`

### 4.3 套餐管理功能测试

**步骤**：
1. 点击左侧菜单"套餐管理"
2. 查看套餐列表
3. 添加新套餐

**截图要点**：
- 截图7：套餐列表页面
- 截图8：添加套餐表单
- 文件名：`03_07_套餐列表.png`、`03_08_添加套餐表单.png`

---

## 五、接口测试截图（04_接口测试）

### 5.1 使用 Apifox 测试接口

**步骤**：
1. 打开 Apifox 软件
2. 导入接口文档（如果有）
3. 选择一个接口，如"获取菜品列表"
4. 点击"发送"按钮

**截图要点**：
- 截图1：Apifox 接口测试界面
- 截图2：接口请求参数和返回结果
- 文件名：`04_01_apifox接口测试.png`、`04_02_接口返回结果.png`

### 5.2 运行 Pytest 接口自动化测试

**步骤**：
1. 打开命令提示符（CMD）
2. 进入项目根目录：
   ```cmd
   cd d:\small-third\project1\hanye-take-out-main
   ```
3. 安装依赖：
   ```cmd
   pip install pytest requests allure-pytest
   ```
4. 运行接口自动化测试：
   ```cmd
   pytest test_api.py -v
   ```

**截图要点**：
- 截图3：Pytest 接口测试运行结果
- 截图4：生成的 Allure 报告
- 文件名：`04_03_pytest运行结果.png`、`04_04_allure报告.png`

---

## 六、UI 测试截图（05_UI测试）

### 6.1 运行 Selenium UI 自动化测试

**步骤**：
1. 确保前端已启动（`http://localhost:5173`）
2. 打开命令提示符（CMD）
3. 进入项目根目录：
   ```cmd
   cd d:\small-third\project1\hanye-take-out-main
   ```
4. 安装依赖：
   ```cmd
   pip install selenium webdriver-manager
   ```
5. 运行 UI 自动化测试：
   ```cmd
   pytest test_ui.py -v
   ```

**截图要点**：
- 截图1：Selenium 测试运行中，浏览器自动打开并操作
- 截图2：UI 测试全部通过
- 文件名：`05_01_ui测试运行中.png`、`05_02_ui测试通过.png`

---

## 七、性能测试截图（06_性能测试）

### 7.1 启动 JMeter

**步骤**：
1. 打开 JMeter：
   ```cmd
   cd D:\apache-jmeter-5.6.3\bin
   jmeter.bat
   ```

### 7.2 加载测试计划

**步骤**：
1. 在 JMeter 中点击"文件" → "打开"
2. 选择项目中的测试计划文件：
   ```
   hanye-take-out-springboot3/server/src/test/resources/嘉园外卖性能测试.jmx
   ```

**截图要点**：
- 截图1：JMeter 界面，显示加载的测试计划
- 文件名：`06_01_jmeter测试计划.png`

### 7.3 运行性能测试

**步骤**：
1. 点击绿色"播放"按钮开始测试
2. 等待测试完成

**截图要点**：
- 截图2：JMeter 测试运行中
- 截图3：聚合报告结果（响应时间、吞吐量等）
- 截图4：后端控制台显示处理请求
- 文件名：`06_02_jmeter运行中.png`、`06_03_聚合报告.png`、`06_04_后端请求日志.png`

---

## 八、安全测试截图（07_安全测试）

### 8.1 使用 OWASP ZAP 扫描

**步骤**：
1. 打开 OWASP ZAP 软件
2. 输入目标地址：`http://localhost:8081`
3. 点击"快速扫描"

**截图要点**：
- 截图1：OWASP ZAP 扫描界面
- 截图2：扫描结果（高危、中危、低危漏洞统计）
- 文件名：`07_01_zap扫描界面.png`、`07_02_zap扫描结果.png`

---

## 九、CI 集成截图（08_CI集成）

### 9.1 Jenkins 流水线运行

**步骤**：
1. 打开 Jenkins 控制台（如果已搭建）
2. 找到项目的流水线任务
3. 点击"立即构建"

**截图要点**：
- 截图1：Jenkins 项目列表
- 截图2：流水线运行状态
- 截图3：构建成功界面
- 文件名：`08_01_jenkins项目列表.png`、`08_02_流水线运行中.png`、`08_03_构建成功.png`

---

## 十、页面展示截图（09_页面展示）

### 10.1 各功能模块页面

**步骤**：
1. 登录商家后台
2. 依次访问以下页面：
   - 仪表盘首页
   - 店铺管理
   - 菜品管理
   - 套餐管理
   - 订单管理
   - 数据统计
   - 员工管理

**截图要点**：
- 截图1：仪表盘首页
- 截图2：店铺管理页面
- 截图3：菜品管理页面
- 截图4：套餐管理页面
- 截图5：订单管理页面
- 截图6：数据统计页面
- 截图7：员工管理页面
- 文件名：`09_01_仪表盘.png`、`09_02_店铺管理.png`、`09_03_菜品管理.png`、`09_04_套餐管理.png`、`09_05_订单管理.png`、`09_06_数据统计.png`、`09_07_员工管理.png`

---

## 十一、截图插入到测试文档

### 11.1 截图命名规范

所有截图必须按照以下命名规范命名：

```
{序号}_{子序号}_{描述}.png
```

示例：
- `01_01_mysql启动.png`
- `02_02_单元测试全部通过.png`
- `06_03_聚合报告.png`

### 11.2 在 Markdown 文档中插入截图

**方法1：使用相对路径（推荐）**

在 markdown 文件中使用以下语法：

```markdown
![截图描述](../screenshots/01_项目启动/01_01_mysql启动.png)
```

**方法2：使用绝对路径**

```markdown
![截图描述](file:///d:/small-third/project1/hanye-take-out-main/screenshots/01_项目启动/01_01_mysql启动.png)
```

### 11.3 在 Word 文档中插入截图

**步骤**：
1. 打开 Word 文档
2. 将光标定位到需要插入截图的位置
3. 点击"插入"选项卡 → "图片" → "此设备"
4. 选择截图文件：`d:\small-third\project1\hanye-take-out-main\screenshots\...`

### 11.4 测试报告中需要截图的位置

以下是测试报告中建议插入截图的位置：

| 报告章节 | 建议插入截图 | 截图文件 |
|----------|--------------|----------|
| 1.2 软硬件测试环境 | MySQL启动、Redis启动 | `01_01_mysql启动.png`、`01_02_redis启动.png` |
| 3. JUnit5单元测试 | 单元测试运行结果、Jacoco覆盖率 | `02_02_单元测试全部通过.png`、`02_03_jacoco覆盖率报告.png` |
| 4. 黑盒功能测试 | 菜品管理、订单管理页面 | `03_01_菜品列表.png`、`03_04_订单列表.png` |
| 5. 接口自动化测试 | Pytest运行结果、Apifox测试 | `04_03_pytest运行结果.png`、`04_01_apifox接口测试.png` |
| 6. UI自动化测试 | Selenium测试运行 | `05_01_ui测试运行中.png` |
| 7. 性能测试 | JMeter聚合报告 | `06_03_聚合报告.png` |
| 8. 安全测试 | OWASP ZAP扫描结果 | `07_02_zap扫描结果.png` |
| 10. CI集成 | Jenkins构建成功 | `08_03_构建成功.png` |
| 附录 | 各页面展示截图 | `09_01_仪表盘.png` 等 |

---

## 十二、自动化截图脚本

### 12.1 Python 自动截图脚本

创建文件：`d:\small-third\project1\hanye-take-out-main\auto_screenshot.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
自动化截图脚本
功能：自动打开浏览器访问指定页面并截图保存
使用方法：python auto_screenshot.py
"""

import os
import time
from selenium import webdriver
from selenium.webdriver.chrome.service import Service
from selenium.webdriver.common.by import By
from webdriver_manager.chrome import ChromeDriverManager

# 截图保存目录
SCREENSHOT_DIR = os.path.join(os.path.dirname(__file__), 'screenshots', '09_页面展示')

# 确保目录存在
os.makedirs(SCREENSHOT_DIR, exist_ok=True)

def init_driver():
    """初始化浏览器驱动"""
    options = webdriver.ChromeOptions()
    options.add_argument('--start-maximized')  # 最大化窗口
    options.add_argument('--disable-infobars')  # 禁用信息栏
    options.add_argument('--no-sandbox')       # 禁用沙箱模式
    
    # 创建浏览器驱动
    driver = webdriver.Chrome(
        service=Service(ChromeDriverManager().install()),
        options=options
    )
    return driver

def login(driver):
    """登录商家后台"""
    print("正在登录商家后台...")
    driver.get("http://localhost:5173")
    time.sleep(2)
    
    # 输入账号密码
    username_input = driver.find_element(By.NAME, 'username')  # 根据实际表单调整
    password_input = driver.find_element(By.NAME, 'password')  # 根据实际表单调整
    
    username_input.send_keys('cyh')
    password_input.send_keys('123456')
    
    # 点击登录按钮
    login_button = driver.find_element(By.CSS_SELECTOR, 'button[type="submit"]')
    login_button.click()
    time.sleep(3)
    print("登录成功！")

def capture_screenshot(driver, filename, description):
    """截取当前页面并保存"""
    filepath = os.path.join(SCREENSHOT_DIR, filename)
    driver.save_screenshot(filepath)
    print(f"✓ 已保存截图: {description} -> {filename}")

def main():
    """主函数"""
    driver = None
    try:
        # 初始化浏览器
        driver = init_driver()
        
        # 登录
        login(driver)
        
        # 截图：仪表盘首页
        capture_screenshot(driver, '09_01_仪表盘.png', '仪表盘首页')
        
        # 截图：菜品管理页面
        print("正在访问菜品管理页面...")
        # 点击菜品管理菜单（根据实际页面调整）
        dish_menu = driver.find_element(By.LINK_TEXT, '菜品管理')
        dish_menu.click()
        time.sleep(2)
        capture_screenshot(driver, '09_03_菜品管理.png', '菜品管理页面')
        
        # 截图：订单管理页面
        print("正在访问订单管理页面...")
        order_menu = driver.find_element(By.LINK_TEXT, '订单管理')
        order_menu.click()
        time.sleep(2)
        capture_screenshot(driver, '09_05_订单管理.png', '订单管理页面')
        
        # 截图：数据统计页面
        print("正在访问数据统计页面...")
        stats_menu = driver.find_element(By.LINK_TEXT, '数据统计')
        stats_menu.click()
        time.sleep(2)
        capture_screenshot(driver, '09_06_数据统计.png', '数据统计页面')
        
        print("\n✅ 所有截图已完成！")
        print(f"截图保存位置: {SCREENSHOT_DIR}")
        
    except Exception as e:
        print(f"❌ 出错了: {e}")
    finally:
        if driver:
            driver.quit()
            print("浏览器已关闭")

if __name__ == '__main__':
    main()
```

### 12.2 运行自动化截图脚本

**步骤**：
1. 确保前端已启动（`http://localhost:5173`）
2. 打开命令提示符（CMD）
3. 安装依赖：
   ```cmd
   pip install selenium webdriver-manager
   ```
4. 运行脚本：
   ```cmd
   python auto_screenshot.py
   ```

**注意事项**：
- 如果页面元素选择器不正确，需要根据实际页面调整脚本中的 `find_element` 参数
- 可以使用浏览器开发者工具（F12）查看元素的选择器

---

## 十三、项目快速启动脚本

### 13.1 Windows 启动脚本

创建文件：`d:\small-third\project1\hanye-take-out-main\start_all.bat`

```batch
@echo off
chcp 65001 >nul
echo ========================================
echo     嘉园外卖项目一键启动脚本
echo ========================================
echo.

echo [1/4] 启动 MySQL...
net start MySQL80
if %errorlevel% equ 0 (
    echo     ✓ MySQL 启动成功
) else (
    echo     ✗ MySQL 启动失败，请手动启动
)
echo.

echo [2/4] 启动 Redis...
start "Redis" /D "D:\redis\redis-install-path" redis-server.exe redis.windows.conf
echo     ✓ Redis 启动中...
echo.

echo [3/4] 启动后端服务...
cd /d "d:\small-third\project1\hanye-take-out-main\hanye-take-out-springboot3\server"
start "Backend" java -jar target/hanye-take-out-server-1.0-SNAPSHOT.jar
echo     ✓ 后端服务启动中...
echo     等待10秒让后端完全启动...
timeout /t 10 /nobreak >nul
echo.

echo [4/4] 启动前端服务...
cd /d "d:\small-third\project1\hanye-take-out-main\hanye-take-out-vue3"
start "Frontend" npm run dev
echo     ✓ 前端服务启动中...
echo.

echo ========================================
echo     启动完成！
echo     前端地址: http://localhost:5173
echo     后端地址: http://localhost:8081
echo ========================================
pause
```

**使用方法**：
1. 右键点击 `start_all.bat` → 以管理员身份运行
2. 等待所有服务启动完成
3. 打开浏览器访问 `http://localhost:5173`

---

## 十四、常见问题与解决方案

### 14.1 端口被占用

**问题**：启动时提示端口 8081 或 5173 已被占用

**解决方案**：
```cmd
# 查找占用端口的进程
netstat -ano | findstr :8081

# 杀死进程（将PID替换为实际进程号）
taskkill /F /PID 12345
```

### 14.2 MySQL 连接失败

**问题**：后端启动时提示无法连接 MySQL

**解决方案**：
1. 确认 MySQL 服务已启动
2. 确认用户名密码正确（`root` / `Qq@135791759`）
3. 确认数据库 `hanye_take_out` 已创建

### 14.3 Redis 连接失败

**问题**：后端启动时提示无法连接 Redis

**解决方案**：
1. 确认 Redis 服务已启动
2. 确认 Redis 端口正确（默认 6379）

### 14.4 前端页面空白

**问题**：访问前端页面显示空白

**解决方案**：
1. 确认后端服务已启动
2. 打开浏览器开发者工具（F12）查看控制台错误
3. 检查网络请求是否成功

---

## 十五、截图检查清单

完成所有截图后，请对照以下清单检查：

| 序号 | 检查项 | 状态 |
|------|--------|------|
| 1 | 所有截图都已按命名规范命名 | ▢ |
| 2 | 所有截图都保存在正确的子文件夹中 | ▢ |
| 3 | 截图内容清晰，没有被遮挡 | ▢ |
| 4 | 测试结果截图显示"通过"状态 | ▢ |
| 5 | 截图已插入到测试文档的对应位置 | ▢ |
| 6 | 截图在文档中显示正常（没有破裂链接） | ▢ |

---

**文档结束**

> 如有问题，请联系第6组测试组：陈越涛（组长）、方海秋、韩景尚、梁锦华
> 指导教师：肖振球
