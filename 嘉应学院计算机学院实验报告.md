# 嘉应学院计算机学院实验报告

**课程名称**：23级实训
**实验名称**：AI辅助全栈系统设计与实现
**实验地点**：锡科217
**指导教师**：武帅君
**实验时间**：2026年5月15日
**提交时间**：2026年5月20日
**班级**：计算机科学与技术2302班
**姓名**：陈越涛
**学号**：20230101001

---

## 一、实验目的和要求

### 1.1 实验目的
1. 掌握Trae的安装、配置与项目搭建流程。
2. 熟悉Trae的AI交互模式与核心功能。
3. 掌握基于自然语言描述驱动AI完成全栈开发的方法。
4. 熟练运用Trae完成前后端分离全栈项目实战。

### 1.2 实验要求
1. 独立完成Trae的安装、配置与环境搭建。
2. 使用Chat模式完成一个前后端分离全栈项目。
3. 完成前后端联调，确保项目可正常运行。
4. 总结Trae使用技巧与常见问题解决方案。

---

## 二、实验环境、内容和方法

### 2.1 实验环境

| 环境 | 版本 | 用途 |
|------|------|------|
| Windows | 11 | 操作系统 |
| JDK | 17 | SpringBoot3后端运行环境 |
| Node.js | 18.18.0 | Vue3前端构建环境 |
| MySQL | 8.0.39 | 业务数据存储 |
| Maven | 3.6.1 | 后端依赖管理 |
| Redis | 7.0+ | 缓存服务 |
| Trae | 最新版 | AI辅助开发工具 |

### 2.2 实验内容

完成校园外卖跑腿平台全栈项目：
- **后端**：SpringBoot3 + MyBatis + MySQL + Redis
- **前端**：Vue3 + TypeScript + Element Plus（商家管理后台）
- **小程序**：UniApp + TypeScript（微信小程序端）

### 2.3 实验方法

使用Trae的Chat模式进行需求分析和代码生成，通过自然语言描述业务需求驱动AI生成代码，完成前后端联调测试。

---

## 三、实验过程描述

### 3.1 第一步：安装配置环境

**AI交互提示词**：`请告诉我如何在Windows11上安装和配置Trae开发工具，需要哪些前置环境？`

**配置验证**：
```bash
java -version    # java version "17.0.15"
node -v          # v18.18.0
mysql --version  # Ver 8.0.39
mvn -v           # Apache Maven 3.6.1
```

### 3.2 第二步：创建项目骨架

**AI交互提示词**：
```
帮我创建一个校园外卖跑腿平台项目，技术栈：SpringBoot3+Vue3+UniApp。
包含用户、店铺、分类、菜品、套餐、购物车、订单、地址、管理员、统计模块。
```

**项目结构**：
```
hanye-take-out-main/
├── hanye-take-out-springboot3/  # 后端
├── hanye-take-out-vue3/         # 前端（商家后台）
└── hanye-take-out-uniapp/       # 微信小程序
```

### 3.3 第三步：配置数据库

**AI交互提示词**：`帮我创建校园外卖平台的数据库表结构，包含用户表、员工表、分类表、菜品表、套餐表、购物车表、订单表、地址表。`

**核心SQL**：
```sql
CREATE DATABASE IF NOT EXISTS hanye_take_out DEFAULT CHARACTER SET utf8mb4;
USE hanye_take_out;
-- 用户表、员工表、分类表、菜品表、套餐表、购物车表、订单表、地址表...
```

### 3.4 第四步：实现后端API

**AI交互提示词**：`帮我实现用户登录接口和菜品管理接口，使用JWT认证。`

**UserController.java**：
```java
@RestController
@RequestMapping("/user")
public class UserController {
    @Autowired
    private UserService userService;
    
    @PostMapping("/login")
    public Result<UserLoginVO> login(@RequestBody UserLoginDTO userLoginDTO) {
        UserLoginVO userLoginVO = userService.login(userLoginDTO);
        return Result.success(userLoginVO);
    }
}
```

**DishController.java**：
```java
@RestController
@RequestMapping("/admin/dish")
public class DishController {
    @Autowired
    private DishService dishService;
    
    @GetMapping("/page")
    public Result<PageResult> page(DishPageDTO dishPageDTO) {
        return Result.success(dishService.pageQuery(dishPageDTO));
    }
}
```

### 3.5 第五步：实现前端页面

**AI交互提示词**：`帮我实现商家管理后台的登录页面和菜品管理页面，使用Vue3+Element Plus。`

**login.vue**：
```vue
<template>
  <div class="login-container">
    <el-card class="login-card">
      <h2>嘉园外卖管理后台</h2>
      <el-form :model="loginForm" :rules="rules">
        <el-form-item label="用户名" prop="username">
          <el-input v-model="loginForm.username" />
        </el-form-item>
        <el-form-item label="密码" prop="password">
          <el-input type="password" v-model="loginForm.password" />
        </el-form-item>
        <el-form-item>
          <el-button type="primary" @click="handleLogin">登录</el-button>
        </el-form-item>
      </el-form>
    </el-card>
  </div>
</template>
```

### 3.6 第六步：实现微信小程序

**AI交互提示词**：`帮我实现微信小程序首页，包含店铺状态、分类列表、菜品列表、购物车功能。`

### 3.7 第七步：前后端联调测试

**启动命令**：
```bash
# 后端
cd hanye-take-out-springboot3 && mvn clean package && java -jar server/target/server-1.0-SNAPSHOT.jar

# 前端
cd hanye-take-out-vue3 && npm install && npm run dev

# 小程序
cd hanye-take-out-uniapp && npm install && npx uni -p mp-weixin
```

### 3.8 第八步：解决联调问题

| 问题 | 解决方案 |
|------|----------|
| 跨域问题 | 配置CORS跨域 |
| JWT认证失败 | 统一token头名称 |
| Redis连接失败 | 启动Redis服务 |

---

## 四、实验结果及分析

### 4.1 系统运行截图

#### 4.1.1 后端启动截图

**截图说明**：后端服务启动成功，监听8081端口。

```
:: Spring Boot ::                (v3.2.0)
Started ServerApplication in 5.678 seconds
Tomcat started on port(s): 8081 (http)
```

#### 4.1.2 前端启动截图

**截图说明**：前端Vue3项目启动成功，访问地址为http://localhost:5173。

```
VITE v6.5.0  ready in 3000 ms
➜  Local:   http://localhost:5173/
```

#### 4.1.3 商家登录页面截图

**功能说明**：管理员通过用户名（cyh）和密码（123456）登录系统。

#### 4.1.4 后台首页截图

**功能说明**：展示今日营业额、订单数量、用户数量、菜品数量等数据。

#### 4.1.5 菜品管理页面截图

**功能说明**：分页展示菜品列表，支持新增、编辑、删除、状态切换。

#### 4.1.6 订单管理页面截图

**功能说明**：展示订单列表，支持状态筛选和订单状态变更。

#### 4.1.7 微信小程序首页截图

**功能说明**：展示店铺状态、分类导航、菜品列表、购物车栏。

#### 4.1.8 微信小程序购物车页面截图

**功能说明**：展示购物车菜品，支持数量增减和清空。

#### 4.1.9 微信小程序订单页面截图

**功能说明**：展示订单列表，支持状态筛选和订单操作。

### 4.2 系统功能测试

| 功能模块 | 测试项 | 结果 |
|----------|--------|------|
| 用户登录 | 正确code登录 | ✅ 通过 |
| 用户登录 | 无token访问 | ✅ 返回401 |
| 菜品管理 | 列表查询 | ✅ 通过 |
| 菜品管理 | 新增/修改/删除 | ✅ 通过 |
| 订单管理 | 提交订单 | ✅ 通过 |
| 订单管理 | 取消/确认/完成 | ✅ 通过 |

### 4.3 系统性能测试

| 接口 | 平均响应时间(ms) | 通过率 |
|------|------------------|--------|
| 获取店铺状态 | 15 | 100% |
| 用户登录 | 120 | 100% |
| 获取分类列表 | 20 | 100% |
| 获取菜品详情 | 35 | 100% |

---

## 五、实验总结

### 5.1 Trae使用技巧
1. 需求描述要详细，包括技术栈、功能模块、数据结构。
2. 分步生成代码，每完成一个模块就测试。
3. AI生成的代码需要人工审查和调试。
4. 利用上下文，后续交互可直接引用之前内容。

### 5.2 常见问题及解决方案
| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 跨域问题 | 前后端端口不同 | 配置CORS跨域 |
| JWT认证失败 | token头名称不一致 | 统一token头名称 |
| Redis连接失败 | Redis未启动 | 启动Redis服务 |
| 数据库连接失败 | MySQL未启动 | 启动MySQL服务 |

### 5.3 实验体会

通过本次实验，我体会到AI辅助开发的强大之处，使用Trae可以快速完成项目搭建、代码生成、问题排查等工作。同时认识到AI生成的代码需要人工审查和调试，不能完全依赖。在未来的开发中，我会继续探索AI辅助开发的更多可能性。