# 嘉园外卖自动化测试脚本运行说明

## 目录
1. 环境准备
2. 接口自动化测试运行说明
3. UI自动化测试运行说明
4. JMeter性能测试运行说明
5. 常见问题解决

---

## 1. 环境准备

### 1.1 安装Python环境
```bash
# 安装Python 3.11+
# 下载地址：https://www.python.org/downloads/

# 验证安装
python --version
```

### 1.2 安装测试依赖
```bash
# 安装基础依赖
pip install pytest requests allure-pytest pyyaml

# 安装UI自动化依赖
pip install selenium webdriver-manager

# 安装数据库依赖（可选）
pip install pymysql redis
```

### 1.3 安装Allure报告工具
```bash
# Windows系统
# 下载地址：https://github.com/allure-framework/allure2/releases
# 解压后将bin目录添加到系统环境变量PATH

# 验证安装
allure --version
```

### 1.4 安装JMeter
```bash
# 下载地址：https://jmeter.apache.org/download_jmeter.cgi
# 解压后运行 bin/jmeter.bat
```

---

## 2. 接口自动化测试运行说明

### 2.1 配置文件修改
编辑 `config.yaml` 文件，配置测试环境：
```yaml
# 服务器配置
base_url: "http://192.168.1.100:8081"  # 修改为实际测试地址

# 数据库配置（可选）
database:
  host: "192.168.1.101"
  username: "root"
  password: "Qq@135791759"
```

### 2.2 运行测试
```bash
# 方式1：运行所有测试
pytest test_api.py -v

# 方式2：生成Allure报告
pytest test_api.py --alluredir=allure-results

# 方式3：运行指定模块
pytest test_api.py::TestUserAPI -v

# 方式4：运行指定测试用例
pytest test_api.py::TestUserAPI::test_user_login -v
```

### 2.3 生成报告
```bash
# 生成HTML报告
allure generate allure-results -o allure-report

# 启动报告服务器
allure serve allure-results
```

### 2.4 测试用例结构
```
test_api.py
├── TestUserAPI          # 用户模块测试
│   ├── test_user_login      # 用户登录测试
│   ├── test_get_user_info   # 获取用户信息测试
│   └── test_send_code       # 发送验证码测试
├── TestShopAPI          # 店铺模块测试
├── TestDishAPI          # 菜品模块测试
├── TestCartAPI          # 购物车模块测试
├── TestOrderAPI         # 订单模块测试
└── TestAddressAPI       # 地址模块测试
```

---

## 3. UI自动化测试运行说明

### 3.1 配置文件修改
编辑 `test_ui.py` 文件，修改测试地址：
```python
class LoginPage(BasePage):
    def __init__(self, driver):
        super().__init__(driver)
        self.url = "http://localhost:8080/login"  # 修改为实际地址
```

### 3.2 运行测试
```bash
# 运行所有UI测试
pytest test_ui.py -v

# 生成Allure报告
pytest test_ui.py --alluredir=allure-results-ui

# 运行指定测试类
pytest test_ui.py::TestLogin -v
```

### 3.3 POM页面结构
```
test_ui.py
├── BasePage            # 基础页面类
├── LoginPage           # 登录页面
├── HomePage            # 首页
├── ShopDetailPage      # 店铺详情页
├── CartPage            # 购物车页面
├── OrderPage           # 订单页面
├── TestLogin           # 登录功能测试
├── TestShop            # 店铺浏览测试
├── TestCart            # 购物车功能测试
└── TestOrder           # 订单功能测试
```

---

## 4. JMeter性能测试运行说明

### 4.1 配置修改
1. 打开 `嘉园外卖性能测试.jmx`
2. 修改用户自定义变量：
   - server_ip: 测试服务器IP
   - server_port: 测试服务器端口
   - concurrency: 并发用户数（默认100）
   - duration: 测试时长（默认1800秒）

### 4.2 运行测试
```bash
# 方式1：图形界面运行
# 打开JMeter -> 打开测试计划 -> 点击绿色播放按钮

# 方式2：命令行运行（推荐）
jmeter -n -t "嘉园外卖性能测试.jmx" -l result.jtl -e -o report
```

### 4.3 测试场景说明
| 场景 | 接口 | 权重 |
|------|------|------|
| 首页浏览 | 获取店铺状态、分类列表、菜品列表 | 40% |
| 点餐流程 | 加入购物车、提交订单 | 30% |
| 订单查询 | 订单列表、订单详情 | 20% |
| 用户登录 | 用户登录 | 10% |

### 4.4 性能指标目标
| 指标 | 目标值 |
|------|--------|
| 并发用户数 | 100 |
| 平均响应时间 | <500ms |
| 95%响应时间 | <1000ms |
| 错误率 | <1% |
| QPS | >50 |

---

## 5. 常见问题解决

### 5.1 模块找不到错误
```bash
# 错误信息：ModuleNotFoundError: No module named 'xxx'
# 解决方法：安装对应的模块
pip install xxx
```

### 5.2 Allure报告生成失败
```bash
# 错误信息：Could not find allure commandline
# 解决方法：确保Allure已安装并添加到环境变量
allure --version  # 验证安装
```

### 5.3 JMeter中文乱码
```bash
# 解决方法：修改jmeter.properties文件
# 找到如下配置并修改：
sampleresult.default.encoding=UTF-8
```

### 5.4 Selenium浏览器驱动问题
```bash
# 错误信息：WebDriverException: 'chromedriver' executable needs to be in PATH
# 解决方法：使用webdriver-manager自动管理驱动
# test_ui.py已配置自动下载，无需手动配置
```

### 5.5 端口被占用
```bash
# Windows查看端口占用
netstat -ano | findstr :8081

# 杀死占用进程
taskkill /F /PID <进程ID>
```

---

## 6. 测试文件清单

| 文件名称 | 说明 | 位置 |
|----------|------|------|
| test_api.py | 接口自动化测试脚本 | 项目根目录 |
| test_ui.py | UI自动化测试脚本 | 项目根目录 |
| config.yaml | 测试配置文件 | 项目根目录 |
| 嘉园外卖性能测试.jmx | JMeter性能测试计划 | src/test/resources |
| allure-results | Allure报告数据目录 | 自动生成 |
| allure-report | Allure报告HTML目录 | 自动生成 |

---

## 7. 技术支持

如有问题，请联系测试团队：
- 测试负责人：XXX
- 联系方式：XXX@xxx.com